# 品牌图标提示词（Seedream 用）

这份文档是给**自己去试提示词**准备的：一条能用的完整提示词 + 每句在干什么 + 可以怎么改。
代码里的同一份在 `tools/gen-brand.mjs` 的 `SETS.refstyle`，两处改一处记得同步另一处。

---

## 一、参数

| 项 | 值 | 说明 |
|---|---|---|
| 模型 | `doubao-seedream-4-5-251128` | 4.5。档位**只有 2K / 4K**（3K 是 5.0 Lite 专属，填了会被拒） |
| 尺寸 | `2K` | 最终产物只是 32~512px 的图标，**2K 已经远超所需**；4K 一个像素都用不上，纯烧钱 |
| 比例 | 方图 1:1 | 图标必须方图 |
| 水印 | `false` | 一定要关，否则右下角会带水印 |
| 输出格式 | 固定 JPEG | 4.5 没有 `output_format` 参数（那是 5.0 的），所以**别按 .png 存**，按字节判格式 |
| 单张价格 | 约 **$0.04 / 张（≈ ¥0.3）** | [TheRouter 模型页](https://therouter.ai/zh/models/doubao--doubao-seedream-4-5/)；BytePlus 文档口径 `$0.04/image` |

---

## 二、当前这版提示词（2026-09-23，出图成功）

```
app icon, cute anime whale-girl mascot, head and shoulders bust portrait, centered inside a thin elegant gold ring, a large slim crescent moon curving behind her head, one four-pointed gold star above her, a few small scattered stars, hair is deep muted indigo blue: medium-dark slate blue with softer periwinkle highlights and darker navy shading, clearly darker and greyer than sky blue, absolutely not pale cyan, not light sky blue, not white-bluish, long flowing wavy hair falling over both shoulders, small whale-fin ear ornaments, a small blue whale floating beside her head, big bright blue eyes, gentle smile, white and navy maid headdress with gold trim and a tiny gold star, her head and shoulders fill most of the frame, flat cel-shaded anime illustration, clean bold line art, strong readable silhouette, simple shapes, high contrast, crisp edges, designed to stay recognizable when scaled down to 48 by 48 pixels, perfectly centered square 1:1 composition, the gold ring sits well inside the frame with clear empty margin on all four sides, deep navy blue starry background with a subtle nebula glow, no watermark, no signature, no frame border, no text, no letters, no numbers
```

用本仓库的工具跑（会自动读 `ARK_API_KEY`、按字节判后缀、把原图写进 `assets/brand/raw/<时间戳>/`）：

```powershell
node tools/gen-brand.mjs --dry-run --set refstyle     # 只看提示词，不花钱
$env:ARK_API_KEY = "你的 key"
node tools/gen-brand.mjs --yes --set refstyle          # 真出图（1 次调用）
```

---

## 三、每句在干什么（改的时候别删错）

| 片段 | 作用 | 能不能删 |
|---|---|---|
| `app icon, … bust portrait` | 一开始就定死「这是图标、是胸像」。不写的话模型会画全身站姿，缩到 32px 只剩一根线 | 别删 |
| `centered inside a thin elegant gold ring` | 金环是这套视觉的锚点 | 可删（但会失去识别度） |
| **`hair is deep muted indigo blue: … absolutely not pale cyan, not light sky blue, not white-bluish`** | **本次的关键**。只写 `deep blue` / `dark blue` 完全没用——前几版都出成发白的青色。**颜色词必须成对写：要什么 + 不要什么** | 这是重点，别删 |
| `a small blue whale floating beside her head` | 鲸是品牌元素。放在头侧而不是怀里，缩小时才不会被脸挤掉 | 可删 |
| `small whale-fin ear ornaments` | 鲸鱼耳，参考图里的特征 | 可删 |
| `white and navy maid headdress with gold trim` | 女仆头饰 | 可删 |
| `flat cel-shaded … crisp edges` | 扁平上色、粗描边——小尺寸下唯一能活的画法 | 别删 |
| `designed to stay recognizable when scaled down to 48 by 48 pixels` | 明确要求「缩小后还认得出」 | 别删 |
| `the gold ring sits well inside the frame with clear empty margin on all four sides` | **踩过坑才加的**：第一版没写，模型把金环画到画布边缘（实测包围盒 4087×4095 / 画布 4096），裁圆时四个极值点全被切掉 | 别删 |
| `no watermark, no signature, no frame border, no text, no letters, no numbers` | 文生图模型很爱在「logo / app icon」类提示里自己加一行字母或加个边框 | 别删 |

---

## 四、想改的话，改这几处

- **发色更深 / 更亮**：动 `deep muted indigo blue` 这组词。可用的说法——
  更深：`near-navy indigo with almost black shadows`；
  更亮：`medium cerulean blue with bright sky-blue highlights`。
  **无论怎么改，都把对应的「不要什么」一起写上**（`not pale cyan, not washed out` 之类）。
- **换成全身 / Q 版**：把 `head and shoulders bust portrait` 换成 `full body standing pose` 或
  `chibi super-deformed, big head small body`。但记住：全身在小尺寸下基本只剩轮廓。
- **去掉月亮 / 换元素**：`a large slim crescent moon curving behind her head` 可删；
  也可以换成 `four small tarot suit emblems on the ring`（权杖/圣杯/宝剑/星币，见 `SETS.tarot`）。
- **背景换浅色**：把 `deep navy blue starry background` 换成 `soft warm cream background`。
  注意浅底图标在浅色标签栏上会糊，实测过。

---

## 五、拿到图之后

模型经常自作主张加一圈**圆角卡片白底**（本次这张四角就是白的，内缩 80px 处还是白的）。
接线进站点的流程：

1. 把新图丢进 `assets/brand/raw/<批次>/`（该目录已 gitignore，不进仓库）；
2. **内缩 6% 裁掉白角**（实测裁完四角都是星空），另存成 `assets/brand/mark-<版本>-1024.webp`；
3. 在 `tools/icon-art.mjs` 的候选里加一条 `raster: true` + `src` + `crop`（裁切框是归一化的 x/y/w/h）；
4. `node tools/make-favicon.mjs --only <key> --pick <key>` 写 favicon；
   `node tools/make-icons.mjs --key <key>` 写主屏图标。

只有一件事需要你告诉我：**新图的文件名**，剩下我来接。

---

## 六、版权提醒

参考图（知乎上那张鲸鱼少女，右下角有「知乎 @字母榜」水印）是**别人的作品**：

- 不要拿它做图生图（4.5 支持传参考图编辑，能力上做得到，但不该这么用）；
- 不要把它拷进仓库、也不要放到线上去；
- 只把从它身上**吸出来的色号**记进文档就行（本次：头发中位色 `#757DA6`，
  对比我们那张的 `#90D4F8`，换算成「色相 +21 / 饱和 ×0.70 / 明度 ×0.67」）。

「风格」不受版权保护，所以照这个方向用文字描述去生成没问题；
但直接把那张图当素材用是有风险的。
