# 维护记录

> 给下一位接手的人（或下一次会话的我）。按时间倒序，只记「结论 + 位置 + 坑」。

---

## 2026-09-23 · 全面优化（性能 / 缺陷 / PWA / 无障碍）

### 实测数据（`serve.mjs` + 无头 Edge，1440×900 冷缓存）

| 指标 | 优化前 | 现在 |
|---|---|---|
| 首次访问首屏 | 4.30 MB | **1.16 MB** |
| 二次访问首屏 | 4.30 MB（`no-store`，缓存完全失效） | **0 KB**（304） |
| 打开「牌面图鉴」 | 47 MB / 9.3 s | **0.6 MB** |
| 卡面素材占用 | 45.6 MB | 20.4 MB + 1.3 MB 缩略图 |
| CJK 字体 | 3.98 MB | 847 KB |
| 牌背纹章（压在首屏） | 1.29 MB (2048²) | 120 KB (512²) |

回归脚本：`node tools/verify-e2e.mjs`（19 项全过）、`node tools/verify-breakpoints.mjs`（10 个断点全过）。

### 本次目标内的修复

| 缺陷 | 根因 | 位置 |
|---|---|---|
| **卡面比例错配**（每张牌左右各 2.9px 暗边） | 容器 150×250 = 0.600，画面 300×520 = 0.577，SVG `meet` 缩放后左右留白 | `--card-h: calc(var(--card-w) * 520 / 300)`；四个断点实测均为 0.5769、暗边 0px |
| **扇形牌悬停/抽中抬起全部失效** | `deal-in` 的 `fill: both` 让 `to { transform: none }` 在动画结束后**永久**以动画优先级压掉 `transform`（实测普通声明无效、`!important` 才生效） | 改 `fill: backwards`；`styles/card.css` |
| `.fan .card { transform: translateX(-half-w) }` 定时炸弹 | 重叠扇形时代的遗留，一直被上面那条动画盖住；动画一修就会让整个牌阵平移半张牌宽 | 连同 `--half-w`、`:has(.fan-shell)` 一并删除 |
| 牌义结尾缺标点 | 数据里 1165/1248 段没有句末标点（是统一风格，不是个别疏漏），拼接后读着断 | `reading.js` 加 `closeSentence()`，不改 245 KB 的 `meanings.js` |
| 死代码 | `.slot*` 全组、`.hint-bar`、`.fan-shell`、`dealToFan`、`placeCard`、`countUp`、`createBackOnlyCard`、`cardBackHTML`、`orientationTone`、`clearStage`、`analyse()` 里的死变量 `e` | 已全删；**保留 `@keyframes sigil-flow`**（`whale-back.js` 的 `.dsh-flow` 依赖它，已迁进该文件自己的 `<style>` 消除跨文件隐式耦合） |

### 顺带发现并修掉的 3 个原有 bug

这三个都不在计划里，是回归脚本与截图对比时撞出来的：

| 缺陷 | 根因 |
|---|---|
| **「全部翻开」在 0/3 时就可见** | UA 样式表的 `[hidden]{display:none}` 与 `.btn{display:inline-flex}` 同为 (0,1,0) 优先级，但**作者样式胜过 UA 样式**，`hidden` 被压掉。点下去有 JS 边界判断兜着，所以一直没被发现。修法：`base.css` 加 `[hidden]{display:none!important}` |
| **进度条永远比牌位条少 1**（抽满 3 张显示 2/3，最后一格永远不亮） | `updateProgress()` 被放在翻牌动画**之前**，那一刻这张牌还没进 `state.picks` |
| **移动端 425px 横向溢出**（375px 视口） | `.table-foot` 是 `flex-wrap:wrap`，改成 `column-reverse` 后交叉轴成了水平，`align-items:center`（为 row 写的）让子项退化成内容宽度；`.spread-slots` 的 7 个牌位胶囊撑到 770px。只加 `align-items:stretch` **不够**——wrap 容器的 stretch 依据的是 flex line 的 cross size，而该 line 已被撑到 770px。必须给 `.spread-slots` 显式 `width:100%; min-width:0` |

### 新增

- **PWA**：`manifest.webmanifest` + `sw.js` + `assets/icons/`（3 个图标用无头浏览器渲染，`tools/make-icons.mjs`）+ `src/core/pwa.js`。
  `install` 预缓存壳约 1.5 MB；卡面不预缓存（用到才留）；**代码类走网络优先**，图片字体走缓存优先。
- **局面持久化**：`src/core/session.js`，种子 + 「抽了第几张」存 `sessionStorage`，刷新自动回到原处，牌面完全一致。
- **分享链接**：同一份数据编码进 URL hash（`#r=…`，约 90 字符），解读页有「复制分享链接」按钮。
- **无障碍**：扇形牌加 `role="button"` + `tabindex` + Enter/Space；浮层打开移入焦点、内部循环 Tab、关闭归还；牌背 `aria-label` 去重。
- **抽牌台自适应**：张数同时受视口宽度与**实测**高度约束（9~21 张）；宽且不矮的视口用 0.8 倍大小的牌，900px 高即可一屏放下全部候选牌。
- **构建工具**：`tools/optimize-art.py`（降采样 + 缩略图 + 牌背图，需 Pillow）、`tools/make-font-subset.py`（需 fontTools + brotli）、`tools/make-icons.mjs`、`tools/manifest-lib.mjs`（清单生成逻辑，`serve.mjs` 与 `make-manifest.mjs` 共用）。

### 下次接手要注意

1. **新增中文文案后必须重跑** `python tools/make-font-subset.py`，否则新字回落到系统字体；
   `--check` 能检测缺字。
2. **换了卡面图后**：`python tools/optimize-art.py` → `node tools/make-manifest.mjs`。
   `optimize-art.py` 会**就地覆盖**原图，所以它先检查 `.backup/` 里有没有完整原图副本，没有就拒绝跑。
3. **改了代码却看不到效果**：先怀疑 Service Worker（它会返回旧缓存）。
   DevTools → Application → Service Workers → Unregister，或勾 Bypass for network。
   改了 `sw.js` 的缓存策略就把里面的 `VERSION` 加一。
4. **SW 只在安全上下文生效**：手机通过局域网 IP 访问时不会注册，功能不受影响，只是没有离线。
5. **用无头浏览器写验证脚本的两个坑**（都踩过）：
   - `URL.pathname` 保留百分号编码，中文路径下会凭空造出 `D:\Claudecode%E5%AD%98...`
     目录并把 profile 写进去；要用 `resolve()` 或 `fileURLToPath()`。
   - `font-display: swap` 期间量尺寸，回退字体字宽不同会量出**假的**横向溢出
     （实测报 128~425px，等 `document.fonts.ready` 之后是 0）。
6. 本次装了 Python 包：`pillow` `fonttools` `brotli`（一次性构建依赖，不进站点运行时）。

### 备份

`.backup/20260923-182643/`：完整备份（130 个文件、51.5 MB，含 78 张原图与全部字体），
`.backup/LATEST.txt` 指向它。图片降采样是不可逆的，回滚时从这里按路径覆盖回去。

### 第二轮：按反馈调整（同日）

| # | 反馈 | 根因与处理 |
|---|---|---|
| 1 | 选牌看着难受 | 张数被 `Math.max(9, …)` 从 7 抬到 9，而一行只放得下 7 张 → 最后一行只有 2 张、右侧空一块。现在**张数永远取列数的整数倍**，且列数由 JS 写进 `--fan-cols`，不再交给 CSS `auto-fit` 自己算（两边口径不同迟早对不上）。1440 下现在是 8 列 × 2 行 = 16 张，完全对称 |
| 2 | 洗牌动画删掉或优化 | **已整段删除**。飞牌是 `fixed + z-index:200`，会盖住标题与说明文字；坐标取自 `rectOf(deckEl)`，而刚切屏时 `.screen` 还在入场动画里，量出的位置偏上，牌会飘到屏幕左上角。只保留牌库自身的 `.deck-pump`。`makeFlyer` / `flyTo` / `removeFlyer` / `midPoint` 一并清掉。<br>⚠️ 删掉飞牌后又暴露一个被它盖住的问题：`.deck-area .deck { transform: scale(0.74) }` 会被 `.deck-pump` 这条 transform 动画**整个替换掉**，牌库在洗牌瞬间跳到 1.0 倍（120px）再跳回来。已把缩放改做在 `width/height` 上；顺手删掉同样被覆盖的 `.deck-shake` 死代码 |
| 3 | 缩短翻牌后等待 | 3.0 / 3.8 / 5.0 秒 → **2.2 / 2.8 / 3.6 秒**；并在浮层上加「继续 →」按钮随时提前结束（实测点击到浮层消失 407ms）。按钮单独设 `pointer-events:auto`，浮层本身仍为 `none`，否则会挡住下面的牌 |
| 4 | 主页输入框是否有用 | **有用**：称呼进 `openingLine()` 的开场语，问题显示在解读页顶部副标题。但原 label「仅作记录」措辞误导（并不保存历史），已改为「会用在解读开头」「会显示在解读页顶部」 |
| 5 | 左上角图标回到初始界面 | 它本来就是 `<a href="./">`，但整页重载后 `session.js` 又把牌局恢复回来，所以「点了像没反应」。现在改为不刷新回首页 + 清空局面 + 撤销方向与牌阵选中，抽出 `resetToStart()`（「开始新的占卜」复用同一函数，但保留已选方向省一步点击） |
| 6 | 其他可优化点 | 已顺手做的：解读页「← 重新抽牌」改名为「← 回到牌桌」（原名让人以为要重抽，实际只是回去看牌）；「重洗」加二次确认。其余建议见下 |

**⚠️ 第 2 条的连锁回归**（用户截图发现「牌库压住了下面的说明文字」）：

把 `.deck` 的 0.74 缩放从 `transform: scale()` 改到 `width/height` 之后，`.deck` 的
**布局尺寸**才真正变小（120×208 → 88.8×154），于是牵出两处以前被掩盖的问题：

1. **顶层那张 `.card.deck__layer` 不跟随缩放**。`.card` 自带 `width/height: var(--card-w/h)`，
   而绝对定位元素同时有 `width` 和 `inset:0` 时是 `width` 胜出——所以它仍是 120×208，
   比牌库大出一整圈，底部压住 caption **58px**。
   修法：`.card.deck__layer { width:100%; height:100% }`。
   （用 `transform: scale()` 时 `.deck` 的布局尺寸同样是 120×208，两者一致，所以一直没暴露。）
2. **`.deck-caption` 的 `margin-top:-14px`** 是抵消旧布局尺寸的历史包袱；现在 gap 只有
   10px，这个负 margin 又把文字拉进牌库 4px。已删掉。

回归断言已加进 `tools/verify-e2e.mjs`：**「牌库不遮挡下方的说明文字」**
（直接比较牌库最低边与 caption 顶边）。这类几何重叠只有截图才看得出来，
断言比肉眼可靠——这次的教训是：**改缩放方式时，要连同「谁还依赖着旧的布局尺寸」一起检查**。

**第 6 点里还没做的建议**（按价值排序，供下次决定）：

1. **解读页「总览」位置**：它在牌阵下方很远处，翻完牌的第一屏只有牌、没有结论。
   可把总览提到牌阵上方，或在牌阵底部加一个「跳到总览」的锚点。
2. **图鉴里看不到牌义**：点开某张牌只有放大视图。可在 zoom 面板里补一段牌义摘要——
   用户在选方向之前想先了解某张牌，目前无处可查。
3. **抽牌台没有「撤销上一张」**：抽错只能整局重洗。`state.picks` 是数组，pop 一张并把
   那张牌恢复成未抽状态即可。
4. **新手不知道选哪个牌阵**：8 个方向 × 5 个牌阵 = 40 种组合。可按方向给一句推荐
   （如「抉择」→ 建议「处境·阻碍·行动」）。
5. ~~**占卜历史**：`session.js` 已经存了种子和抽牌位置，扩成 `localStorage`
   的环形缓冲（最近 20 局）即可。~~ **已否决**（2026-09-24，用户明确不要；见第八轮第 2 条）。

### 第三轮：手机连不上 + bat 编码（同日）

**现象**：手机打不开网站。

**真正的原因跟 bat 无关**：当时 4173 上跑的是会话里用 `--local` 起的临时实例，
只绑了 `127.0.0.1`，手机当然连不上。bat 默认就是绑 `0.0.0.0` 的，
防火墙规则（`WhaleOracleTarot 4173`，Inbound Allow）也早就在了。
**排查结论：先用 `Get-NetTCPConnection -LocalPort 4173 -State Listen` 看绑定地址，
`127.0.0.1` = 只有本机能连，`0.0.0.0` = 手机能连。**

**但顺带查出一个真 bug：bat 是 UTF-8 编码，中文全乱码**，还冒出两条
`'xxx' is not recognized as an internal or external command`。

- 根因：`cmd.exe` 解析 `.bat` 用的是**当前代码页**，文件里的 `chcp 65001` 要执行到那一行
  才生效，而它自己那一行之前的中文已经按旧代码页解析了。UTF-8 的多字节汉字一旦跨越
  cmd 的读取缓冲区边界就会字节错位，把半个汉字当成命令执行。
  **这就是「启动 bat 老是出错」的根因**——说明文字越长越容易触发。
- 修法：**bat 存成 GBK(936)**，文件开头不做 chcp；只在调用 Node 之前
  `chcp 65001 >nul`（Node 的输出是 UTF-8），Node 返回后 `chcp 936 >nul` 切回来。
  实测中文正确、无解析错误、服务正常起在 `0.0.0.0`。
- ⚠️ **以后编辑这个 .bat 千万不要用默认 UTF-8 保存**，否则问题立刻复现。
  写入方式（PowerShell）：
  `[System.IO.File]::WriteAllText($p, $c, [System.Text.Encoding]::GetEncoding(936))`

**`tools/serve.mjs` 同时做了三处改进**（都是这次排查中发现的坑）：

1. **端口被占用时自动往后找一个空闲端口**。以前是未处理的 `error` 事件 → 直接抛栈退出，
   表现就是「本机能开、手机怎么都连不上」而用户完全看不出为什么。
2. **区分真局域网地址与虚拟网卡**。这台机器上有 Radmin VPN（`26.32.248.148`），
   也会被列成「手机：」，用那个 IP 手机永远连不上。现在标成
   「虚拟网卡，手机连不上」，二维码也只指向真局域网地址（192.168 / 10. / 172.16-31）。
3. **排查提示从 1 条扩到 3 条**：IP 是否用错（VPN 网卡）→ 防火墙入站 → 路由器 AP 隔离。

### 顺带做了 C 盘瘦身（与站点无关）

清理 TEMP 陈旧垃圾 338 MB；npm 缓存 807 MB、pip 缓存 44 MB 迁移到 `D:\cache\`；
`TEMP`/`TMP` 用户环境变量改到 `D:\Temp`（**需重启 DSH 与终端才生效**）。C 盘 10.5 GB → 11.8 GB。

### 第四轮：手机操作逻辑 + 解读页排布 + 字号（同日）

**用户反馈**：①手机上「双击选项后可以直接跳转下一步」；②手机最终解读界面排布要优化；
③手机 UI 偏小，可能出现文字偏移或缺失。

新增回归脚本 `node tools/verify-mobile.mjs`：4 个设备（360×740 / 390×844 / 414×896 / 768×1024 竖屏平板）
× 5 个界面（方向 / 牌阵 / 抽牌台 / 解读 / 解读浮层），逐屏统计**横向溢出、<12px 的文字、
<40px 的触控目标、被裁的文字**，并单独量解读网格的**列数 / 行数 / 宽度利用率**。
用 `#r=` 分享链接直达「已抽满」的局面，省掉 3×6 秒的抽牌动画。

**① 连点两次选项 = 直接进入下一步**

- `src/main.js`：`isSecondTap(kind, key)` —— 同一个选项在 **1.6 秒**内被点第二次就前进。
  第一次点击仍然只做选中，所以既不误触、也不影响「先点 A 看看、再改点 B」。
  8 个方向 / 5 个牌阵共用一套逻辑；`startDraw()` 从 `#toDraw` 的监听里抽出来给两处复用。
- **`touch-action: manipulation`**（`base.css`）：不加这条，第二次点按会被浏览器识别成
  「双击缩放」，页面先放大一下再跳转，还平白多出约 300ms 点击延迟。真机上必然踩。
- 发现性靠一次性 toast：`已选「事业」· 再点一次直接进入牌阵`，每步只提示一次。
- 牌阵 → 抽牌加了一把 `shuffling` 锁：连点之后页脚那颗按钮还在原位，紧接着再点一次
  会让两次 `runShuffle` 的 await 交错。

**② 解读页排布（这是本次最大的一处）**

| | 改前 | 改后 |
|---|---|---|
| 390px 手机 3 张牌 | 写死 `repeat(2, 104px)` → **2+1 两行**，左右各空 100px | `repeat(3, minmax(0,1fr))` → **一行三张**，宽度利用率 **100%** |
| 牌宽 | 104px | **106.6px**（列宽自适应） |
| 5 张 / 7 张 | 同样 2 列 | 3 列（3+2）/ 4 列（4+3） |

- 列数由 JS 按张数写进 `--reading-cols`（`paintReadingGrid()`），列宽交给 `1fr`。
  旧的三档媒体查询（116 / 104 / 94px）连同 `@media (max-width:760px)` 里 celtic/cross 的
  两列覆盖**一并删除**——它们正是「牌小 + 末行落单」的来源。
- 牌面高度改用 `aspect-ratio: 300 / 520`，**没有**再用 `--card-w` / `--card-h` 那对 px 令牌：
  它们是一组固定值，只改宽度高度不跟着变，会得到畸形比例（和牌桌那次踩的是同一个坑）。
- 单牌阵单独收窄到 232px 并居中，否则一张牌会铺成 400×690。
- 解读页四颗按钮在手机上改 **2×2 等宽网格**（`.reading-foot`），
  「⧉ 复制分享链接」这种长文案不再被压到换行。

**③ 字号 / 触控 / 裁切**

| 项 | 改前 | 改后 |
|---|---|---|
| 浮层关闭键 | 34×34 | **44×44** |
| 顶栏图标按钮（≤480px 只留图标时） | 38×42 | **44×44** |
| 主按钮 / 次级按钮高度 | ~40px | **44 / 40px** |
| `.cat__en` / `.deck-caption` / 牌位序号 / 图鉴牌名 | 10.5 / 11 / 11 / 9px | **12 / 12 / 12 / 12px** |
| `.key-card span` / `.stat span` / 品牌副标题 | 11.5 / 11.5 / 10px | **12px** |
| `.summary-block li` / `.doc li` | 14.5px | **15px** |

- **覆盖必须放在 `app.css` 末尾**：媒体查询不加优先级，同优先级按源码顺序决胜，而
  `.summary-block li` / `.disclaimer` / `.gallery__name` / `.deck-caption` 都定义在
  `app.css` 中间——把覆盖写回 `base.css` 会被它们反压回去（第一版就是这么写的，实测没生效）。
- `html { text-size-adjust: 100% }` 关掉 iOS 的字体自动放大。
- `.cat__name` / `.spread__name` / `.reading-card__name` 等加 `overflow-wrap: anywhere`，
  用户输入的英文长串不会再把卡片顶宽。
- 补 `env(safe-area-inset-bottom)`：`.toast`、`.reveal-caption`、页脚内边距。

**④ 手机页脚主按钮常驻**（`position: sticky; bottom: 0`）

选完方向/牌阵，主按钮就在拇指下，不必再滚到页底去找。
页脚本体 `pointer-events: none`（只让半透明背板不挡住上面的输入框），按钮单独打开命中测试。
负边距用新的 `--gutter` 变量而不是写死 `-16px`——`#app` 的内边距是
`clamp(14px, 4vw, 46px)`，写死会在 360px 视口多出 2px 把文档顶宽（审计报「出界」，实测过）。
顺带把 `.toast` 抬到 `bottom: 86px`：留在 22px 处会和常驻页脚叠在一起互相盖住。

**⑤ 解读浮层加「上一张 / 下一张」**

`index.html` 加 `#detailNav`，`main.js` 拆出 `renderDetail(i)`（填内容）与 `openDetail(i)`
（额外负责开浮层）。分开是必要的：浮层已经打开时再走一遍 `openModal` 会把 `lastFocused`
记成浮层内部的元素，关闭后焦点无处可还。键盘 `←` `→` 同样可用。
手机上读三张牌不用再「关掉 → 点下一张 → 再等动画」。

**验证**：`verify-e2e.mjs` 27/27、`verify-breakpoints.mjs` 10/10、`verify-mobile.mjs` **问题 0 条**
（16 行全 0）。截图在 `.shots/mb/`。

**审计脚本自己的三个坑**（记下来，改脚本时别再犯）：

1. 直接比 `scrollWidth` 判「文字被裁」会把 `.btn--primary::after` 那条扫光
   （`left:-60%` → `130%`）算成内容，于是 `#againBtn` 稳定误报「287px 挤在 165px 盒子里」。
   改用 `Range.getBoundingClientRect()` 量**文字自己**的范围。
2. 判「被裁」必须要求该轴 `overflow: hidden/clip`。`overflow: visible` 时
   `scrollHeight > clientHeight` 是正常的（行盒比 glyph 盒矮、标题用 `background-clip: text`），
   早先这么写制造了十几条误报。
3. 分享链接要先 `Page.navigate('about:blank')` 再跳过去：只差 hash 属于**同文档导航**，
   浏览器不会重新加载，`boot()` 也就不会去读 `#r=`。

**顺带：字体子集缺字 + `--check` 自己会崩**

- 这一轮往 `src/**/*.js` 里写了中文注释，`python tools/make-font-subset.py --check`
  报出 **15 个新增字符**（`⚠兀兜册吞宁拦播栈沿访邻颗飘驻`）。扫描范围是
  `index.html` + `src/**/*.js` 的**全部字符**（含注释），所以注释也会把 `--check` 打红。
  这是刻意的保守做法——不解析语法就不会被字符串里的 `//`（URL）骗到；
  代价只是十几个字形，重跑一次即可。已重跑：**1826 字 / 汉字 1662，CJK 字体合计 852 KB**。
- ⚠️ **同时把 `sw.js` 的 `VERSION` 从 `v2` 提到 `v3`**。字体走的是缓存优先，
  不换 VERSION 的话已装过 PWA 的设备会一直用旧子集，新字继续回落系统字体。
- 修了脚本自身一个 bug：`--check` 打印 `⚠` 时在中文控制台（GBK 936）抛
  `UnicodeEncodeError` 直接崩，而崩溃点就在「列出缺哪些字」那一行，等于看不到报告。
  已在开头把 `sys.stdout/stderr` 固定成 UTF-8 + `errors="replace"`。
  **这套工具里凡是往终端打中文的 Python 脚本都可能踩同一个坑。**

### 第五轮：顶栏品牌标记换 Seedream 徽记 + 换成 4.5 省钱（同日）

**需求**：改左上角那个图标（= 网页图标），用 Seedream 按我写的提示词出一张新的。

**做了什么**

| 项 | 结果 | 位置 |
|---|---|---|
| 出图脚本 | 新建 `tools/gen-brand.mjs`。刻意不并进 `gen-art.mjs`：那边是 78 张竖版、提示词由牌义推导、写进 `assets/tarot/`；这边是**一张方图**、固定几个构图候选、写进 `assets/brand/` | `tools/gen-brand.mjs` |
| 裁切与对照 | 新建 `tools/brand-fit.py`。按**金环包围盒**（通道运算，不是逐像素）自动裁切，并出真实尺寸对照表（180/96/64/44px） | `tools/brand-fit.py` |
| 顶栏 | `#brandMark` 改渲染位图，**先挂矢量版、`onload` 之后再替换**；失败就保持矢量版。容器从 44px 圆角方改成 **48px 圆形**（裁掉四角空背景，脸占满整格） | `src/main.js` `mountBrandMark()`、`styles/base.css` |
| favicon | 换成同源母题：深色圆角方 + **粗新月 + 四角星**（去掉细金环，见下） | `index.html`、`tools/make-favicon.mjs` |
| 资产 | 最终只留 `assets/brand/mark.webp`（256px，19.3 KB） | `assets/brand/` |

**踩到的三个坑**

1. **Ark 返回的是 JPEG，我按 `.png` 落盘了。** 后缀和真实格式不符，`read_image` 直接拒开、
   Pillow 也给出误导性的 format。改成按**字节魔数**判扩展名。
2. **新月画法错了，图形整个消失且不报错。** 用「两条弧过同一对端点」时，弦长已等于外弧直径，
   内弧半径给小了会被 SVG 规范**自动放大到同半径**，两条弧完全重合、面积归零。
   第一版渲染出来 a 只剩一个金环、b 几乎空白。改用「大圆挖掉一个偏移圆」（`mask`）。
3. **`encodeURIComponent` 不转义 `'`**（规范里它属非保留字符），而 SVG 属性引号全是单引号——
   手写 `'${svg}'` 拼进 JS 会在 `xmlns='…'` 处被截断，表现为「渲染失败」而看不出原因。
   要用 `JSON.stringify` 包。

**favicon 的取舍（16px 实测）**：一开始想直接缩新徽记，做了三版对照表后否掉——
细金环在 16px 会糊成一圈毛边，动漫脸更是认不出。最后定成「粗新月 + 四角星」，
单一实心高对比形状。**对照表比肉眼可靠：三张候选在 64px 都好看，只有 16px 那一列能分出高下。**

**⚠️ 成本事故（必须记下来）**

- 用 `size=4K` 跑了 **6 张 4096²**（第一批纯角色 3 张 + 第二批加塔罗元素 3 张）。
  两个错误叠加：**① 第一批在用户明确要「加塔罗元素」之前就出了，那 3 张直接被弃用；
  ② 4K 毫无必要——最终产物只是顶栏 256px 的 WebP。**
- 后果：`e2e` 的「首屏 ≤ 1.3 MB」被顶到 1.49 MB（512px PNG 占 316 KB）。
  换 256px WebP 后 19.3 KB，首屏回到 **1.20 MB**，27/27 全过。
- 已加防呆：**`gen-brand.mjs` 真实调用必须显式 `--yes`**，否则打印计划后 exit 3，
  一个请求都不发。默认尺寸从 4K 降到 **2K**。

**模型换成 Seedream 4.5**（`doubao-seedream-4-5-251128`）

- 三处默认模型同步改：`tools/serve.mjs`（生成台）、`tools/gen-art.mjs`（卡面）、`tools/gen-brand.mjs`。
- **参数规则不能照搬**，按[官方速查表](https://github.com/a86582751/doubao-seedream-image-skill/blob/main/references/api-quickref.md)核对：
  4.5 的档位只有 **2K / 4K**（`3K` 是 5.0 Lite 专属，传了会拒），
  显式像素串下限同为 2560×1440，且**固定输出 JPEG**（`output_format` 只有 5.0 有）。
  已把生成台的 `sizeOptions` 里的 `3K` 删掉。
- **README 里原来那句「4.5 与 5.0 规则完全相同」是错的**，已按速查表逐条改正。
- 顺带发现：`assets/tarot/` 只留下两份 `_report*.json`，**都是 5.0 Lite 且都是「跳过」**——
  78 张主体的产出模型没有留底，而 09-21 的记录写的是 4.5。整副牌大概率是混着来的，
  README 里那句「78 张都是 5.0 出的」已改成实测到的说法。

**这次出图实际用的模型**：`doubao-seedream-5-0-260128`（5.0 Lite）+ `size=4K`，共 6 次调用。
**4.5 是这之后才换上的默认值**，不会追溯影响已经出的那 6 张——现在顶栏那张徽记确实是 5.0 Lite 画的。

### 第六轮：`.shots/` 堆了 1.3 GB 垃圾（同日）

**起因**：用户反馈「右侧栏目看不见图片」。

**查到的东西**（按重要性）

1. **`.shots/` 里堆了 26,466 个文件 / 1,519 MB。** 每个验证脚本都用无头 Edge，
   每次都在 `.shots/` 下建一个**完整的 Chromium 用户数据目录**（`*-profile`），
   脚本只在**开头** `rm -rf`，跑完从不清理。累计 **44 个 profile 目录、23,804 个文件、1.17 GB**。
   已清理 45 个目录，**释放 1,321.6 MB**（1,519 → 197.7 MB，文件数 26,466 → 295）。
   保留的是真正的截图与对照表（`brand/` `bp/` `mb/` `e2e/` `flow/` `frames/` 等，共 289 MB）。
2. **`.shots/probe/` 下还嵌着三个 profile**（`edge-bp` / `edge-diag` / `edge-verify`）。
   第一遍按顶层目录判定时漏掉了——它们是**二级目录**，所以分类逻辑必须往下钻一层。
3. **已从根上修掉**：六个脚本的 `PROFILE` 全部改到 `join(tmpdir(), 'dsh-*-profile')`，
   工作区再也不会有 profile。改的是 `verify-e2e` / `verify-breakpoints` / `verify-mobile` /
   `make-icons` / `make-favicon` / `drive`。跑完复验：`.shots` 仍是 295 个文件，
   新 profile 出现在 `%TEMP%`（各约 60 MB）。

**顺带查出的两个真问题（都不是本次现象的原因，但该记）**

- **`~/.dsh/profiles/node_modules` 整棵树都是断链**：482 个 junction 全部无效、0 个正常。
  它们指向 `C:\Users\黄文\AppData\Local\npm-cache\_npx\1e7f6d9597241db0\node_modules\@deepseek-ai\…`，
  而那个 `_npx` 存储在**原位置和迁移后的 `D:\cache\npm` 里都已经不存在**——
  是之前 C 盘瘦身搬 npm 缓存留下的。桌面版不受影响（它从
  `D:\DS Harness\DSH Desktop Beta\resources\app\node_modules` 加载，已用进程命令行确认），
  但 **npx 形式的 `dsh` CLI 会踩到**。web profile 有 `.dsh-module-fallback` 兜底。
- **README 里「78 张卡面都是 5.0 出的」不成立**：`assets/tarot/` 只留下两份 `_report*.json`，
  都是 5.0 Lite 且都是「跳过」（`ok=0`），主体没有留底；而 09-21 的记录写的是 4.5。

**排除掉的**：不是 `maxFileBytes` 的问题——`dsh-api-workspace-files` 的上限是 **32 MiB**，
我生成的图最大才 1.2 MB。图片预览插件在桌面版 `node_modules` 里完好可读。

### 第七轮：右侧栏打开文件一片空白（DSH 环境侧，同日）

**现象**：右侧栏里点开图片文件，预览区空白（破图图标 + alt 文本）。用户口述「看不见图片」。

**排查走过的弯路**（记下来省得下次重走）

1. 先查了 `C:\Users\黄文\.dsh`——**那是错的 home**。`DSH_HOME=D:\.dsh`，
   前者是旧 npx 装法的残骸（482 个 junction 全断链、0 个正常）。真插件在
   `D:\.dsh\profiles\web\node_modules`，而且是好的。
2. `profile-selection/state.json` 写着 **`"active": "web"`**——桌面版加载的也是 web profile，
   所以 `dsh-better-sidebar` 确实挂着。
3. 拿真请求打 `http://127.0.0.1:43120/sidebar/file?...` 全返回 403 `forbidden`，
   一度以为栅栏有问题。**是误导**：正文是 9 字节的 `forbidden`，来自侧栏自己的 fence，
   因为我的请求没有浏览器的会话；浏览器那边是好的（文件树能用就是证据）。

**拿到真相的办法：给那条路由加临时日志。** 往 `lib/index.js` 注入 `dbgMedia()`
（`appendFileSync` 写 `D:\dsh-sidebar-debug.log`），备份原文件，让用户重启一次、重现一次。
日志直接给出答案：

```
REQ   sid=session-1f84… cwd=D:\Claudecode存储\网页塔罗占卜 path=.shots/brand/sheet.png
CWD-RESOLVED cwd=D:\Claudecode存储\网页塔罗占卜 fence=true
ERROR ".shots/brand/sheet.png" is not an absolute path
```

**根因**（`dsh-better-sidebar@0.19.1`，**已是最新版**）

1. DSH 记录「本轮产出的文件」时，**工作区内的文件存的是相对路径**
   （`dsh-workspace-changes` 文档原话：「位于工作目录内时为相对路径，否则为绝对路径」）。
2. 侧栏 `intercept.tsx:19` 会调 `resolveSidebarPath(summary?.cwd, path)` 转绝对，
   但该函数有条**静默失败**分支：`const base = cwd ?? ''; if (base === '') return path`
   —— 会话列表快照取不到 cwd 时，把相对路径原样丢下去。
   （日志证明客户端在 `cwd` 参数上知道正确路径，只有 `path` 是相对的。）
3. 服务端 `path-security.ts`：`resolveSessionPath` **只做 WSL 的 `/foo` 投影，
   从不把相对路径拼到 cwd**，紧接着 `requireAbsolute()` 抛错 → 400 → 预览空白。

**影响面不只是图片**：侧栏里打开**任何**产出文件都走这条路径，文本文件同样打不开。

**修法**：服务端容忍相对路径、按会话 cwd 解析（与客户端 `resolveSidebarPath` 语义一致，
且服务端本就拿得到正确 cwd）。`lib/index.js` 里三处同一模式全改：

```js
const projected = resolveSessionPath(cwd, target);
const absolute = requireAbsolute(isAbsolute(projected) ? projected : join(cwd, projected));
```

绝对路径仍走原逻辑，行为不变；只补上「客户端给了相对路径」这一种情形。

**⚠️ 这个补丁不可持久**：改的是插件目录里的**构建产物**。实测一次插件卸载/重装
（`.plugin-manager/logs` 里 22:33 与 22:36 两次 pnpm 操作）就把补丁连同我留的 `.bak` 一起清掉了。

所以做成幂等脚本 **[tools/patch-sidebar-relpath.mjs](tools/patch-sidebar-relpath.mjs)**：

```bash
node tools/patch-sidebar-relpath.mjs            # 应用（已打过则跳过）
node tools/patch-sidebar-relpath.mjs --check    # 只看状态
node tools/patch-sidebar-relpath.mjs --revert   # 还原
```

插件被重装覆盖后重跑一次即可；**改完必须重启 DSH 桌面版**（宿主只在启动时加载该文件）。
真正的修复应提给上游。

---

### 第八轮：移除首屏输入框 + 模型权限收窄 + favicon 二版（2026-09-24）

**1. 右侧栏图片预览：已确认修好。** 重启后用户实测可以浏览图片，本项结案。
补丁位置与重打方法见第七轮。它仍然是 `node_modules` 里的改动——
插件一旦被重装/更新就会退回原样，症状是「打开任何产出文件都是空白」。

**2.「占卜历史」从来没有实现过。** 用户说「去掉」，核实后确认它只是第二轮
「还没做的 5 条建议」里的第 5 条，代码里没有这个东西——`src/core/session.js` 是
**单局**持久化 + 分享链接（`sessionStorage` 一份 + URL hash 一份），不存历史。
本轮把它从建议列表里划掉，不做。

**3. 首屏底部的「称呼 / 问题」输入框已移除**（`index.html` 里的 `.field-row`）。
连带清理：

- `src/main.js`：`dom.userName` / `dom.userQuestion` 的读取与回填全部删掉；
  `state.userName` / `state.question` 两个字段**保留**，只用来接**旧分享链接**里的
  `n` / `q`，让 2026-09-24 之前发出的链接仍能显示原问题和开场称呼；
- `styles/base.css`：`.field-row` / `.field` / `.field__label` / `.field input` 全部删除，
  `touch-action` 名单里的 `.field input` 一并删掉；`styles/app.css` 里的手机版
  `.field__label` 字号规则也删掉；
- `tools/drive.mjs`：原来往这两个输入框灌值的两行，改成**断言它们确实不存在**
  （`report.legacyInputs` 必须为 0），防止以后有人不小心加回来。

回归：e2e **27/27**、断点 **10/10**、手机审计 **问题 0 条**，`verify.mjs` 通过。
`sw.js` 的 `VERSION` 由 `v3` 提到 **`v4`**（本轮最后停在 `v5`，见第 7 条），
否则手机上缓存优先的旧壳还会被命中。

**4. 方舟 Key 的调用权限已收窄到只允许 Seedream 4.5。**
Key 本身没换，是后台权限改了——**再调 5.0 / 3.0 系列会直接报错**。
`tools/gen-art.mjs`、`tools/gen-brand.mjs`、`tools/serve.mjs` 的默认模型本来就已经是
`doubao-seedream-4-5-251128`，本轮只把 `tools/studio.html` 里那段写着
「形如 `doubao-seedream-5-0-lite`」的提示改成 4.5 + 2K/4K，免得照着填然后报错。

**5. favicon 定版：候选 `g`——品牌插画本身（光栅）。**
中间过程值得记：我先按「16px 只有厚、亮、单一实心形状能活」的实测结论推荐了矢量候选
`i`（金月牙 + 四角星），用户看完对照表要的是 `g`。**用户的判断优先**，而且 `g` 在 32px
以上确实好看得多——代价是 16px 会退化成「一颗带颜色的圆点」，这是尺寸的物理限制，
已在 README 里如实写明，并保留 `--pick i` 一键换回的路。
`make-favicon.mjs` 本轮重写：四角星改成参数化（v1 是写死的路径）、`--only` 支持
`a,h,i` 这样的逗号列表、光栅候选改走**渐进折半**降采样（1024→512→…→16，比一步缩到底干净）。
`index.html` 的 `<link rel="icon">` 也从 SVG data URI 换成 **32px PNG 的 data URI**（约 4 KB），
`favicon-16/32.png` 一起重出（1.0 KB / 3.0 KB）。
对照表：`.shots/brand/favicon-v2.png`。

**6. 新增 `tools/icon-art.mjs`：图标图形的唯一来源。** favicon 与 PWA 图标从此共用同一份
候选清单——之前 `make-icons.mjs` 是抓页面顶栏的 `#brandMark`（鲸鱼娘徽记）、
`make-favicon.mjs` 是自己另画一套，结果是「标签页一个样、主屏图标另一个样」。
两边各留一份拷贝迟早漂移，所以抽成模块：**改 `icon-art.mjs` = 两边一起变**
（`DEFAULT_KEY` 表示当前选用哪个）。
`make-icons.mjs` 因此大幅简化：不再打开站点、不再注入 `#iconStage` 截图，
直接把图形扔进 `<img>` 再 `drawImage` 进 canvas 取 PNG。

**7. 光栅候选支持 + PWA 图标重做。**
- `make-icons.mjs` 现在两条路都走：矢量候选把 SVG 画进 canvas；光栅候选（`g`）走
  「夜空竖直渐变打底 + 圆形裁切的插画 + 渐进折半」。
- 新增 `assets/brand/mark-1024.webp`（`brand-fit.py --size 1024` 从 4096 原图裁的，134 KB）：
  512 图标拿 256 放大会发虚。`MARK_SOURCES` 优先取它，没有才退回头栏那份 256。
- PWA 图标已重出为插画版（192 / 512 / maskable-512）；maskable 内容收到 62%
  （落在中心 80% 直径安全区内），`any` 版 90%。
- **两次覆盖都留了档**（这项目没有 git，覆盖前必须留档）：
  `.backup/20260924-pwa-icons/` = 最早的鲸鱼娘版；
  `.backup/20260924-pwa-icons-crescent/` = 中间那版金月牙。换回去拷回来即可。
- 代价：PWA 图标从 83 KB 涨到 586 KB，`sw.js` 预缓存的「壳」由约 1.5 MB 变成约 2 MB，
  README 与 `sw.js` 注释都已同步。
- `sw.js` 的 `VERSION` 本轮从 `v3` 一路提到 **`v6`**（输入框移除、金月牙版、插画版各一次）。

**8. 纠正上一轮的错误结论。** 之前说
`C:\Users\黄文\.dsh\profiles\node_modules` 下有「482 个断链 junction」——**这是错的**。
实测：482 个重解析点，**断链 0 个**，目标全部指向
`C:\Users\黄文\AppData\Local\npm-cache\_npx\1e7f6d9597241db0\node_modules`（该目录存在），
整个遗留 home 合计不到 0.1 MB。它只是旧 npx 安装留下的空壳（`DSH_HOME=D:\.dsh`），
删不删都不影响使用，**不是需要修的东西**。

---

### 第九轮：品牌图标定版（用户自出图）+ AI 生成内容标识（2026-09-23 晚）

**1. 图标定到 `n3`：用户自己出的一版插画，三处统一。**
这轮来回挺多，记一下路径免得下次重走：最初的插画发色偏浅青 → 用户要「更像参考图」→
试了①同一张图的不同裁切（j/k/l）②程序调色压深（`tools/brand-recolor.py`，`mark-deep`/`mark-deeper`）
③从参考图吸色号做配色迁移（`mark-ref`，中位色 `#90D4F8` → `#757DA6`，色相 +21 / 饱和 ×0.70 / 明度 ×0.67）
→ 还是不够 → 用 Seedream 4.5 重出一张（`mark-v3`）→ **最后用户自己出了一张更好的**（`mark-v4`）。
经验：**发色这种要求，重出一张比在旧图上调色省事得多**；调色只适合「整体偏一点」。
`icon-art.mjs` 的 `DEFAULT_KEY` 现在是 `n3`（头 + 鲸 + 星星取景），
favicon、主屏图标、顶栏（`mark-v4.webp`）三处一致。

**2. 新图源与候选规范。**
`assets/brand/mark-v4-1024.webp` 由 2048² 原图直接缩到 1024（全幅、无白边可裁）。
顺带定了条规矩：**光栅候选一律显式写 `src`**，不再依赖 `MARK_SOURCES` 的默认值——
默认值会随「当前用哪张」变，历史候选会莫名其妙换图（这轮差点踩到）。

**3. 水印与合规——这轮最需要记住的一件事。**
用户那张原图右下角有平台强制的「AI生成」水印（在 App / 网页里出图就有）。
用户要求「去掉水印」，**没有照做**：《人工智能生成合成内容标识办法》（2025-09-01 施行）
明确禁止恶意删除、篡改、伪造、隐匿生成合成内容标识。
改成两件正确的事：
- **取景避开**（不是抹除）：水印在 x 0.81~0.97 / y 0.90~0.96，所有裁切下界 y ≤ 0.75；
  顶栏用整幅但圆形遮罩会把四角切掉。16px 的图标物理上也放不下那个框——属于正常构图裁切。
- **补上站点的显著标识**（下面第 4 条）。
另外实测确认：用 API 出图（本仓库脚本传了 `watermark: false`）**出来的图本来就没有水印**，
EXIF 0 条、无任何 AI 元数据；用户那张的水印来自 App 端，换 API 出图即可避免。

**4. 加了 AI 生成内容标识（两处）。**
- `src/data/rules.js` 的 `DISCLAIMER`：末尾补「本站塔罗插画（78 张）与品牌图标均由 AI 生成
  （火山方舟 Seedream），解读文案由 AI 辅助撰写后人工校订」，渲染在每份解读底部；
- `src/main.js` 的 `renderHelp()`：新增「六、关于 AI」一节，写在「规则 ⓘ」弹窗里。

**5. 字体子集重新生成。** 新文案带进 6 个新字（恒抠撰署舟著），
`make-font-subset.py --check` 抓出来了——**这一步别忘**，否则那几个字会静默回落到系统字体。
重跑后 1832 字 / 汉字 1668，覆盖完整。

**6. 顺手修了 `make-favicon.mjs` 一个假警报。**
`--pick` 挑的就是当前那张时，替换结果与原文一致，脚本用 `next === src` 判断，
于是误报「index.html 里没匹配到 `<link rel="icon">`」，把人骗去查模板。
现在先取出旧 href 再比较，分「已经是这张」和「真的没匹配到」两种情况。

**回归**：e2e **28/28**（本轮新增「AI 生成标识两处都在」一条，从 27 涨到 28）、
断点 **10/10**、手机审计 **问题 0 条**、`verify.mjs` 通过。
`sw.js` 的 `VERSION` 由 `v7` 提到 **`v8`**。

⚠ 那条新断言第一版**写错了位置**：放在了「点 logo 回首页」之后，那时解读页连同
`.disclaimer` 已被清掉，于是自己给自己报了个假失败。挪到分享链接检查之后（还在解读页上）
才是对的——**断言也要挑时机**。

---

### 第十轮：上线到 GitHub Pages（2026-09-23 深夜）

**站点已上线**：https://tohka-v.github.io/whale-oracle-tarot/
仓库：https://github.com/Tohka-v/whale-oracle-tarot （公开）

**1. 先做了一次「能不能放子目录」的改造。**
GitHub Pages 项目页给的是 `用户名.github.io/仓库名/`，也就是**子目录**，而原来的路径
全是绝对路径，在那个地址下必坏。改了三处：
- `sw.js`：新增 `BASE = new URL('./', self.location).pathname`，预缓存列表与所有路径判断
  都改成相对 BASE；`cache.put('/index.html')` 之类也一并改掉；
- `src/core/pwa.js`：`register('./sw.js')`（scope 交给浏览器按脚本位置推导）；
- `manifest.webmanifest`：`start_url` / `scope` / 图标 src 改成相对。

**顺带抓到一个真 bug，只有子目录部署才会暴露**：
`src/data/art-manifest.js` 的 `ART_BASE`/`THUMB_BASE` 和 `src/art/whale-back.js` 的
`BACK_MEDALLION` 都是写死的 `'/assets/…'`——放子目录后它们会去请求域名根，
**图鉴 78 张缩略图 + 每张牌的牌背曼陀罗全部 404**。
原注释写着「这样 tools/*.html 预览页也能用」，但 `'assets/…'` 纯相对又会跟着页面走、
在 tools/ 下同样 404。正解是**从模块自身位置算**：

```js
export const ART_BASE = new URL('../../assets/tarot/', import.meta.url).href;
```

生成器 `tools/manifest-lib.mjs` 一起改了并重新生成清单（除时间戳外只有那几行变化）。
**验证方式**：把站点拷到子目录、用 `serve.mjs` 起在 `/site/`，跑
`verify-e2e.mjs --site http://127.0.0.1:4175/site/`（为此给 e2e 加了 `--site`）。
另外顺手修了 e2e 自己的一个假警报：它用 `getRegistration('/')` 问 scope，
子目录下永远问不到，会误报「未注册」。

**2. 发布链路：Actions 构建 `dist/`，而不是发布整个仓库。**
`tools/build-dist.mjs` 只把网站本体组装进 `dist/`（带**引用自检**：从 index.html /
fonts.css / sw.js 抠出全部本地引用逐个验存在，实测 37 个），
`.github/workflows/deploy.yml` 负责构建 + `deploy-pages`。
不选「Deploy from a branch」是因为那会把 `tools/`、`维护记录.md` 一起公开。
产物 23.9 MB，其中 22 MB 是 78 张卡面。

**3. 两个坑（都不在代码里，记下来省得下次重查）。**

- **`gh` 的 token 默认没有 `workflow` 权限**。第一次推送被拒：
  `refusing to allow an OAuth App to create or update workflow '.github/workflows/deploy.yml'
  without 'workflow' scope`。因为是一次原子推送，**其余 249 个文件也一起没上去**。
  解法：`gh auth refresh -h github.com -s workflow` 再推。
- **本机代理把 `github.com` 分到了直连**：`api.github.com` 通（HTTP 200），
  `github.com` 的 TLS 握手被重置，于是 `gh auth login` 报
  `Post "https://github.com/login/device/code": EOF`，`curl` 报
  `schannel: failed to receive handshake`。
  判断方法（值得记住）：用 CONNECT 分别探两个域名的 443 ——
  **隧道建得起来（200 Connection established）但 TLS 被重置，就是分流规则的问题，不是墙也不是 gh 的锅**。
  用户客户端是 Clash 内核（`com.vortex.helper`，控制口 `127.0.0.1:39797`），
  在有控制接口的情况下可以直接 `PATCH /configs {"mode":"global"}` 切模式（这次读的时候用户已经自己切了）。

**4. 上线验收：把完整的 28 项 e2e 直接打在线上站上。**
给 `verify-e2e.mjs` 加了 `--proxy host:port`（转成 Edge 的 `--proxy-server`，
**不碰系统代理设置**），然后：

```
node tools/verify-e2e.mjs --site https://tohka-v.github.io/whale-oracle-tarot/ --proxy 127.0.0.1:7897
→ 28 项通过 / 0 项未通过
```

关键几条：首屏 **808 KB / 32 请求**、Service Worker 在 https 下注册并激活
（scope = `https://tohka-v.github.io/whale-oracle-tarot/`）、离线可打开、AI 标识两处都在、无 JS 异常。
**「本地全绿」和「线上全绿」是两件事**，这一步值得每次都做。

**5. 上线之后仍然要注意的**：

- 手机访问线上地址是 **https**（安全上下文），所以**手机端这次也能注册 SW、也有离线能力**了
  ——之前在局域网 http 下是没有的。
- GitHub Pages 在国内访问不稳定，要做正式站点建议后面绑自定义域名 + 前置 CDN；
  国内主机/CDN 需要备案，而**占卜类内容的备案有被卡的风险**，选域名和主机时先把这一点想清楚。
- 站点已按《人工智能生成合成内容标识办法》在「规则」弹窗与解读页声明了 AI 生成内容（第九轮）。

---

## 2026-09-21

### 本项目（塔罗站）

| 项 | 状态 | 位置 |
|---|---|---|
| 卡面插画 | **78 / 78** 全齐（Seedream 4.5 补齐最后 7 张星币） | `assets/tarot/` |
| 牌背 | 改黑金纹章；中央曼陀罗为生成图 | `src/art/whale-back.js` + `assets/back/medallion.jpg` |
| 顶部序号带 | 改 `latin`：大阿卡纳与小阿卡纳统一罗马数字，宫廷牌用 `PAGE/KNIGHT/QUEEN/KING` | `src/art/card-art.js` `badgeFor()` |
| 铭牌文字 | 序号带「国王」显示成「国」已修；花色小字压大字已修（基线 452→445） | `card-art.js` |
| **【重要】解读页无金框/无铭牌底框** | 已修。根因：同一张牌在扇形与解读页各渲染一次 → **SVG 内部 id 重复**，`url(#…)` 解析到落在 `display:none` 屏幕里的第一个 → 引用失效 | `renderCardFront(card, { uid })`，`ui.js` 发唯一 uid |
| 启动脚本 | **修好**：`cd /d "%~dp0.."` → `%~dp0`（`%~dp0` 自带尾反斜杠，加 `..` 会跑到上一级）；加 `chcp 65001` 解决中文乱码；改 CRLF、去 BOM | `启动服务器.bat` |

启动方式：**双击 `启动服务器.bat`**（独立窗口，不受 DSH 会话回收影响）。
不要再让 AI 用后台任务起服务——被回收三次了。

### DSH 环境侧（不属于本项目，但会影响使用）

| 项 | 说明 |
|---|---|
| **`dsh-better-sidebar` 已装好** | 官方 3 步流程会**漏注册**：`dsh plugin` 是 pnpm 纯透传，第 1 步 pnpm 失败时已写入 `dependencies` 但没写 `dsh.profile.bundles`，第 3 步就成了 no-op。**手动补进 bundles 末尾**才对。验证命令：`dsh --profile web --dump-config` |
| **插件页文字看不清 → 已修** | 根因：皮肤把 `--dsw-alias-bg-base` 设为 `transparent` 且对话窗格透明，而插件页根节点 `section[data-plugin-panel]` 自身**没有 background** → 浅色文字压在插画上。补丁给该选择器加了 `--dsw-alias-bg-layer-3` 底色 |
| ⚠️ **该补丁不持久** | 改的是 `node_modules` 里的皮肤文件，**皮肤更新或 `pnpm install` 会覆盖**。备份：`client.js.bak-20260921-183438`。要永久生效需做成独立客户端插件（未做） |

皮肤文件：`~/.dsh/profiles/web/node_modules/@smalltailqwq/dsh-client-ui-skin-maid-atelier/lib/client.js`

### 已知未做

- 可读性补丁改成独立插件（见上）
- `assets/back-refs/`（在 `.shots/`，已 gitignore）：未采用的备选纹章图
- Ark API Key 曾以明文出现在会话记录里，建议在火山控制台轮换一次
